
public class ArrayEx1 {
    int[] score = new int[9];
    byte[] values = new byte[10];

    public static void main(String[] args) {
        ArrayEx1 obj = new ArrayEx1();
                
        // Inicializando a matriz score com valores específicos
        obj.score[0] = 10;
        obj.score[1] = 20;
        // ... adicione os valores restantes do array score

        // Inicializando a matriz values com o valor 1 em todas as entradas

        for(int i = 0; )


    }
}
